<main>
    <div class="popupContainerOuter displyScreenResolution  ApplyNow scrollPopup newHomePagePopup homeCrossPopup"
        id="ApplyNowPopUpNEW">

        <div class="popupContainer">
            <div class="fromTophead">

                <h2 class="font-56 font-bold font-black1  letter-spacing1" id="form_heading">
                    Download Placement Report
                </h2>

                <div class="popUpCloseBtnWrap homeCrossfix">
                    <img  src="https://files.mastersunion.link/resources/svg/crossIcon.svg"
                        class="invert-img" alt="Icon" onclick="hideApplyNowPopUp();">
                </div>
            </div>
            <div class="PopupformWrapper" id="leadformContainer">
                <div class="left mob-hide">
                    <img  src="https://images.mastersunion.link/brochures/18022025/v1/StudentsWork.webp" alt="MU Students">
                </div>
                <div class="right">
                    <div class="formContainer active">
                        <div class="flexInput" id="formain">
                            <div class="inputGroup">
                                <label for="name" class="font-14 font-medium font-black1">Enter Email ID <span
                                        class="font-red">*</span></label>
                                <input type="text" placeholder="Enter your email"
                                    class="inputBox font-16 font-black1 font-medium " id="emailReport"
                                    onblur="validateEmailInput('emailReport', 'emailReportVal')">
                                <p class="font-12 font-red font-regular warning" id="emailReportVal">Please Enter Valid
                                    Email
                                </p>
                            </div>

                            <div class="inputGroup">
                                <label for="" class="font-14 font-medium">Enter Mobile Number <span
                                        class="font-red">*</span></label>
                                <div class="flexbox gap10 wd-100 align-stretch">
                                    <div class=" newPhoneCode">
                                        {{!-- <label for="" class="font-14 font-medium">Country Code <span
                                                class="font-red">*</span></label> --}}


                                        <select name="" id="selectCountryCode" class="inputBox safariFix">
                                            <option value="IN">91 (India)</option>
                                        </select>
                                        {{!-- <p class="font-12 font-red font-regular warning" id="countryCodeVal">
                                            Please enter a valid phone number
                                        </p> --}}
                                    </div>

                                    <div class="mob-wd-70" style="width: 70%;">
                                        <label for="" class="font-14 font-medium"> <span
                                                class="font-red"></span></label>

                                        <input type="text" placeholder="Enter Mobile Number*"
                                            oninput="this.value = this.value.replace(/[^0-9]/g, '')"
                                            class="inputBox font-16 font-black1 font-medium " id="numberReport"
                                            maxlength="10"
                                            onblur="validateMobileInput('numberReport', 'numberReportVal')">

                                        {{!-- <p class="font-12 font-red font-regular warning" id="mobileUserVal">Please
                                            enter a valid number
                                        </p> --}}
                                    </div>
                                </div>
                                <p class="font-12 font-red font-regular warning" id="numberReportVal">Please Enter Valid
                                    Number
                                </p>

                            </div>

                            <div class="inputGroup">
                                <label for="name" class="font-14 font-medium font-black1">Enter Name <span
                                        class="font-red">*</span></label>
                                <input type="text" placeholder="Enter your name"
                                    class="inputBox font-16 font-black1 font-medium " id="nameReport"
                                    onblur="validateNameInputV2('nameReport', 'nameReportVal')">
                                <p class="font-12 font-red font-regular warning" id="nameReportVal">Please Check the
                                    input</p>
                            </div>
                            <div class="inputGroup">
                                <label class="font-14 font-medium" for="">Select current total work experience <span
                                        class="font-red">*</span></label>
                                <select name="" id="workExpUser"
                                    
                                    class="inputBox safariFix"
                                    onblur="validateNameInputV2('workExpUser', 'workExpVal', 'Work Experience')">
                                    <option value="" disabled selected>Select current total work experience*</option>
                                    <option value="Fresher">Fresher</option>
                                    <option value="Less than or equal to 2 years">Less than or equal to 2 years</option>
                                    <option value="Greater than 2 and less than or equal to 5 years">Greater than 2 and
                                        less than or equal to 5 years</option>
                                    <option value="Greater than 5 and less than or equal to 8 years">Greater than 5 and
                                        less than or equal to 8 years</option>
                                    <option value="More than 8 and less than or equal to 15 years">More than 8 and less
                                        than or equal to 15 years</option>
                                    <option value="Greater than 15 years">Greater than 15 years</option>
                                </select>

                                <p class="font-12 font-red font-regular warning " id="workExpVal">Work Experience cannot be empty
                                </p>
                            </div>
                           
                            <div class="inputGroup hide">
                                <label class="font-14 font-medium" for="">Enter current organization/college name (if
                                    fresher) <span class="font-red">*</span></label>
                                <input class="inputBox input font-16 font-black1 font-medium " type="text" name=""
                                    id="userOrganization"
                                    placeholder="Enter current organization/college name (if fresher)*">
                               <p class="font-12 font-red font-regular warning" id="workExpVal" style="display: none;">Please select work experience </p>
                            </div>
                        </div>

                    </div>
                    <div class="consentCheckWrapper">
                        <div class="consentCheckRow">
                            <input type="checkbox" id="consentCheckDpr2">
                            <label for="consentCheckDpr2">I authorize Masters&rsquo; Union and its affiliates to process my personal data to send me information about programs related to my application, admission, or enrollment through digital channels (including email, phone calls, RCS, SMS, and WhatsApp), in accordance with Masters&rsquo; Union <a href="https://mastersunion.org/privacy-policy" target="_blank">Privacy Policy</a>, <a href="https://mastersunion.org/cookie-policy" target="_blank">Cookie Policy</a>, and <a href="https://mastersunion.org/terms-and-conditions" target="_blank">Terms &amp; Conditions</a>. By submitting my details, I agree to receive communications related to my application, events, and program updates. Message frequency may vary and message and data rates may apply. I may withdraw my consent at any time by replying STOP, UNSUBSCRIBE, or HELP for assistance.</label>
                        </div>
                        <p id="consentCheckErrorDpr2" class="font-12 font-red font-regular warning consentCheckError">Please accept the consent to continue</p>
                    </div>
                    <div class="formFooter" id="submitbuttonreport">
                        <div class="gradientButton whiteHover" onclick="onButtonClickSubmitReport()">
                            <button class="button iconButton gtmUgDwnBroPop" id="gtmUgDwnBroPopId">
                                Download
                                <svg width="24" height="24" viewBox="0 0 24 24" fill="none"
                                    xmlns="https://www.w3.org/2000/svg">
                                    <path
                                        d="M16.0208 9.41421L7.41421 18.0208L6 16.6066L14.6066 8H7.02082V6H18.0208V17H16.0208V9.41421Z"
                                        fill="white"></path>
                                </svg>
                            </button>
                        </div>
                    </div>
                </div>
            </div>

            <div id="SuccessMessage" class="congrats-box">
                <img src="https://files.mastersunion.link/assets/img/thankyou_logo.svg" alt="Icon">

                <h3 class="thankyouboxHeading mt-10 gtmUgDwnBroGotit" id="gtmUgDwnBroGotitId">Thank You</h3>
                <p class="text-2 gtmUgDwnBroGotPara" id="gtmUgDwnBroGotParaId">You've successfully downloaded our
                    Placement Report</p>
                <p class="text-2">For any queries please contact us at <a class="font-red"
                        href="mailto:admissions@mastersunion.org">admissions@mastersunion.org</a></p>

            </div>
        </div>

    </div>
</main>

<script>


    function pgpPlacementReport(formTitle, destination) {
        const form = document.getElementById("ApplyNowPopUpNEW");
        const h2 = document.querySelector("#form_heading");
// Reset form fields when popup opens
document.getElementById("nameReport").value = "";
document.getElementById("emailReport").value = "";
document.getElementById("numberReport").value = "";
document.getElementById("workExpUser").value = "";
document.getElementById("userOrganization").value = "";

// Remove any validation warnings if present
document.getElementById("nameReportVal").classList.add("warning");
document.getElementById("emailReportVal").classList.add("warning");
document.getElementById("numberReportVal").classList.add("warning");
document.getElementById("workExpVal").classList.add("warning");
if (document.getElementById("consentCheckDpr2")) document.getElementById("consentCheckDpr2").checked = false;
if (document.getElementById("consentCheckErrorDpr2")) document.getElementById("consentCheckErrorDpr2").classList.add("warning");


        form.classList.add("active");
        h2.textContent = formTitle || h2.textContent;
        form.setAttribute("data-destination", destination);
        form.setAttribute("data-title", h2.textContent);

        document.querySelector("#SuccessMessage").classList.add("hide");
        document.querySelector("#submitbuttonreport").classList.remove("hide");
        document.querySelector("#leadformContainer").classList.remove("hide");
        document.querySelector("#form_heading").classList.remove("hide");
    }
    // let name = $('#nameReport').val();
    // let email = $('#emailReport').val();
    // let number = $('#numberReport').val();
    function onButtonClickSubmitReport() {
        console.log('on submit::', full_utm_url);

        // Validate functions
        const validateNumber = (number) => /^\d+$/.test(number);
        const validateEmail = (email) => email.includes("@") && email.includes(".");

        let name = $('#nameReport').val().trim();
        let email = $('#emailReport').val().trim();
        let number = $('#numberReport').val().trim();
        const workExpUser = document.getElementById('workExpUser').value;
        const userOrganization = document.getElementById('userOrganization').value.trim();
        const countryCode = document.getElementById('selectCountryCode').value.trim();


        let isValid = true;

        isValid = validateNameInputV2('nameReport', 'nameReportVal') && isValid;
        isValid = validateEmailInput('emailReport', 'emailReportVal') && isValid;
        isValid = validateMobileInput('numberReport', 'numberReportVal') && isValid;
        // Work experience validation
        if (workExpUser === "") {
            document.getElementById('workExpVal').classList.remove("warning");
            isValid = false;
        } else {
            document.getElementById('workExpVal').classList.add("warning");
        }

        // Organization isValidation
        //  if (userOrganization === "") {
        //      document.getElementById('userOrganizationVal').classList.remove("warning");
        //      isValid = false;
        //  } else {
        //      document.getElementById('userOrganizationVal').classList.add("warning");
        //  }
        //
        if (!document.getElementById('consentCheckDpr2')?.checked) { document.getElementById('consentCheckErrorDpr2').classList.remove('warning'); isValid = false; } else { document.getElementById('consentCheckErrorDpr2').classList.add('warning'); }
        if (!isValid) return;

        const urlSearchParams = new URLSearchParams(window.location.search);
        const params = Object.fromEntries(urlSearchParams.entries());
        const utmSource = params.utm_source || 'Website';
        const utmMedium = params.utm_medium;
        const utmCampaign = params.utm_campaign;
        const gclid = params.gclid;
        const fbclid = params.fbclid;
        const utmCampaignId = params.utm_campaign_id;
        const utmAdGroupId = params.utm_adgroup_id;
        const utmCreativeId = params.utm_creative_id;
        let url = window.location.pathname.substring(1);
        let formname = document.querySelector("#ApplyNowPopUpNEW").getAttribute("data-title") || "Download Placement Report";
        console.log(formname, "form anem22")
        console.log(formname, "form name");

        let medium = `PGP-TBM Apply-Now-Page - ${formname} Popup`;
        let formLocation = `PGP-TBM Apply-Now-Page- ${formname} Popup`;
        let course;
        let field_program_eligible;


         if (formname == "Download Placement Report PGP-TBM") {
             course = "PGP TBM intake of 2025";
             field_program_eligible = "PGP-TBM";
              medium = `PGP-TBM Apply-Now-Page - ${formname} Popup`;
              formLocation = `PGP-TBM Apply-Now-Page- ${formname} Popup`;

         } else if (formname == "Download Placement Report PGP-TBM YLC") {
             course = "PGP TBM - Young Leaders Cohort";
             field_program_eligible = "Young Leaders Cohort";
            medium = `PGP-TBM YLC Apply-Now-Page - ${formname} Popup`;
            formLocation = `PGP-TBM YLC Apply-Now-Page- ${formname} Popup`;

         } else {
              field_program_eligible = "PGP-TBM";
             course = "PGP TBM intake of 2025";  //Default case to handle unexpected values
              medium = `PGP-TBM Apply-Now-Page - ${formname} Popup`;
              formLocation = `PGP-TBM Apply-Now-Page- ${formname} Popup`;
         }


        if (workExpUser === "Fresher") {   // condition changed as if user select fresher the lead will always go to the YLC 
            course = "PGP TBM - Young Leaders Cohort";
            field_program_eligible = "Young Leaders Cohort";
            medium = `PGP-TBM YLC Apply-Now-Page - ${formname} Popup`;
            formLocation = `PGP-TBM YLC Apply-Now-Page- ${formname} Popup`;
        } else {
            field_program_eligible = "PGP-TBM";
            course = "PGP TBM intake of 2025"; // Default case to handle unexpected values
            medium = `PGP-TBM Apply-Now-Page - ${formname} Popup`;
            formLocation = `PGP-TBM Apply-Now-Page- ${formname} Popup`;
        }


        console.log(course, "Final course--::", field_program_eligible);


        //if (url.startsWith("pgp-rise")) {
        //    course = 'PGP Rise';
        //    medium = `PGP Rise - ${formname} Popup`;
        //} else if (url.startsWith("ug-")) {
        //    course = 'Undergraduate Programme';
        //    medium = `UG - ${formname} Popup`;
        //}

        let device = 'PC';
        if (window.matchMedia("(max-width: 768px)").matches) {
            device = "Mobile";
        } else {
            device = "Desktop";
        }

        // api for our Data base
        var post_url = '{{apiBaseURL}}/api/courseRegister';
        var request_method = 'POST'; //get form GET/POST method
        $.ajax({
            url: post_url,
            type: request_method,
            contentType: "application/json",
            data: JSON.stringify({
                "formId": 82,
                "email": email,
                "name": name,
                "mobile": number,
                "countryCode": countryCode,
                //   "organisation": userOrganization,
                "workExperince": workExpUser,
                "courseName": course,
                "full_utm_url": full_utm_url || "N/A",
                "leadSource": formLocation,
                "medium": utmMedium || medium,
                "campaign": utmCampaign || null,
                "source": utmSource,
                "consentCheck": document.getElementById('consentCheckDpr2')?.checked || false,

            }),
            dataType: "json",
            success: function (result) {
                console.log("Second api after first success")
                const post_url = 'https://api-lead.mastersunion.org/api/manageLeads/PGPTBMDownloadBrochure';  // changed for the PGP TBM leads only
                const request_method = 'POST';

                $.ajax({
                    url: post_url,
                    type: request_method,
                    contentType: "application/json",
                    data: JSON.stringify({
                        formId: 82,
                        device: device,
                        email: email,
                        name: name,
                        phone: number,
                        countryCode:countryCode,
                        medium: utmMedium || medium,
                        campaign: utmCampaign || null,
                        source: utmSource,
                        full_utm_url: window.location.href,
                        leadOrigin: formLocation,
                        gclid: gclid||"",
                        fbclid: fbclid||"",
                        utmCampaignId: utmCampaignId||"",
                        utmAdGroupId: utmAdGroupId||"",
                        utmCreativeId: utmCreativeId||"",
                        totalExperience: workExpUser,
                        consentCheck: document.getElementById('consentCheckDpr2')?.checked || false,
                    }),
                    dataType: "json",
                    success: function (result) {

                    }
                });

            }
        });

        const form = document.getElementById("ApplyNowPopUpNEW");
        console.log("all the done");
        localStorage.setItem("userName", name);

        const destination = form.getAttribute("data-destination") || "https://files.mastersunion.link/assets/1/Placement_Report_25.pdf";
        /// const destination2 = "/pgp-placement-thankyou";
        const destination2 = "https://files.mastersunion.link/assets/1/Placement_Report_25.pdf";

        fetch(destination)
            .then(response => {
                if (!response.ok) {
                    throw new Error('Network response was not ok');
                }
                return response.blob();
            })
            .then(blob => {
                ///const url = URL.createObjectURL(blob);
                ///const link = document.createElement("a");
                ///link.href = url;
                ///link.download = "placementreport2024.pdf";
                ///document.body.appendChild(link);
                ///link.click();
                ///document.body.removeChild(link);
                ///URL.revokeObjectURL(url);
            })
            .catch(error => console.error('Error downloading the file:', error));

        const linkPage = document.createElement("a");
        setTimeout(() => {
            linkPage.href = destination;
            linkPage.target = "_blank";
            document.body.appendChild(linkPage);
            linkPage.click();
            document.body.removeChild(linkPage);
            //window.location.href = destination2;
            // Open the PDF in a new tab
            // window.open(destination, "_blank");
            //window.open(destination2, "_blank");
            // Redirect to the thank-you page
            //   window.location.href = "/pgp-placement-thankyou";
        }, 1500);


        document.querySelector("#SuccessMessage").classList.remove("hide");
        document.querySelector("#submitbuttonreport").classList.add("hide");
        document.querySelector("#leadformContainer").classList.add("hide");
        document.querySelector("#form_heading").classList.add("hide");


    }






    function hideApplyNowPopUp() {
        let applyNowPopUp = document.getElementById("ApplyNowPopUpNEW");
        if (applyNowPopUp) {
            applyNowPopUp.classList.remove("active")
        }

        $('#nameReport').val("");
        $('#emailReport').val("");
        $('#numberReport').val("");
        if (document.getElementById("consentCheckDpr2")) document.getElementById("consentCheckDpr2").checked = false;
        if (document.getElementById("consentCheckErrorDpr2")) document.getElementById("consentCheckErrorDpr2").classList.add("warning");

    }


  //  function validationOnblurPgpTbmCommon(field, errorId, errorMessage) {

    //var input = $("#" + field);
   // var error = $("#" + errorId);
   // var value = input.val();

   // if (!value || value.trim() === "") {
   //     error.text(errorMessage);
     //   error.show(); // show error if not selected
   // } else {
   //     error.hide(); // hide error if valid selection made
   // }
//}

function validateSelectField(fieldId, errorId, errorMessage) {
    var input = $("#workExpUser");
    var error = $("#workExpVal");
    var value = input.val();
    let isValid = true;

    if (!value || value.trim() === "") {
        error.text("Please select work experience");
        error.show(); // or use .css({ display: "block", opacity: "1" });
        isValid = false;
    } else {
        error.hide();
    }

    return isValid;
}















    $(document).ready(function () {
        (function deferCscUntilPlacementPopupOpen() {
            var el = document.getElementById("ApplyNowPopUpNEW");
            if (!el) return;
            var started = false;
            function kick() {
                if (started || !el.classList.contains("active")) return;
                started = true;
                getAllCountries();
            }
            kick();
            if (typeof MutationObserver !== "undefined") {
                new MutationObserver(kick).observe(el, {
                    attributes: true,
                    attributeFilter: ["class"],
                });
            }
        })();

        $('#selectCountryCode').change(function () {
            var countryCode = $(this).val();
            getAllStates(countryCode);
        });

        $('#userState').change(function () {
            var countryCode = $('#selectCountryCode').val();
            var stateCode = $(this).val();
            getCities(countryCode, stateCode);
        });




function getAllCountries() {
    $.ajax({
        url: 'https://api.mastersunion.org/api/geo/countries',
        type: 'GET',
        headers: {
            "Accept": "application/json"
        }
    }).done(function (response) {
        $('#selectCountryCode').empty().append($("<option value='' disabled selected></option>").text("Select your country"));

        $.each(response, function (idx, country) {
            // Add + if missing
            let phoneCode = country.phonecode;
            if (!phoneCode.startsWith('+')) {
                phoneCode = '+' + phoneCode;
            }

            $('#selectCountryCode').append($("<option></option>")
                .attr("value", country.iso2)
                .text(`${phoneCode} (${country.name})`));
        });

        $('#selectCountryCode').val('IN').change();
    });
}


        function getAllStates(countryCode) {
            $.ajax({
                url: `https://api.mastersunion.org/api/geo/countries/${countryCode}/states`,
                type: 'GET',
                headers: {
                    "Accept": "application/json"
                }
            }).done(function (response) {
                $('#userState').empty().append($("<option value='' disabled selected></option>").text("Select your state"));
                $.each(response, function (idx, state) {
                    $('#userState').append($("<option></option>")
                        .attr("value", state.iso2)
                        .text(state.name));
                });
            });
        }

        function getCities(countryCode, stateCode) {
            $.ajax({
                url: `https://api.mastersunion.org/api/geo/countries/${countryCode}/states/${stateCode}/cities`,
                type: 'GET',
                headers: {
                    "Accept": "application/json"
                }
            }).done(function (response) {
                $('#userCity').empty().append($("<option value='' disabled selected></option>").text("Select your city"));
                $.each(response, function (idx, city) {
                    $('#userCity').append($("<option></option>")
                        .attr("value", city.name)
                        .text(city.name));
                });
            });
        }
    });

</script>